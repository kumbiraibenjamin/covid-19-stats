export interface Country {
    name: string;
    code: string;
    emoji: string;
    unicode: string;
    image: string;
}

// Added from this URL https://cdn.jsdelivr.net/npm/country-flag-emoji-json@2.0.0/dist/index.json